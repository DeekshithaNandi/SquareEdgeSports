package com.squareedgesports.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class EmailConfig {
    // Email configured via application.properties / application-prod.properties
}
